import { transferGold } from 'reducers/common';
import { getPlayerPath } from 'stateUtils';

const messenger = (state, player) => transferGold(
    state,
    ['treasury'],
    [...getPlayerPath(state, player), 'resources'],
    8
);

export default messenger;
